export const Footer = ({ children }: { children: React.ReactNode }) => {
  return (
    <footer className="text-jerma-light-blue/50 border-t border-jerma-light-blue/20 text-center py-4 absolute bottom-0 w-full">
      {children}
    </footer>
  )
}
